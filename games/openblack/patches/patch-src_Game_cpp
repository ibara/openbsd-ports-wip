$OpenBSD$

Index: src/Game.cpp
--- src/Game.cpp.orig
+++ src/Game.cpp
@@ -75,7 +75,7 @@ Game::Game(int argc, char** argv)
 	args.Get("w", windowWidth);
 	args.Get("h", windowHeight);
 
-	std::string binaryPath = fs::path{argv[0]}.parent_path().generic_string();
+	std::string binaryPath = "${LOCALBASE}/share/openblack";
 	spdlog::info("current binary path: {}", binaryPath);
 	{
 		std::string gamePath;
