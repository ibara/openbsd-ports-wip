$OpenBSD$

Don't build the Linux 80386 backend.
Would require a BDEP on an i686-linux-gnu-gas package.

Index: toolchains.lua
--- toolchains.lua.orig
+++ toolchains.lua
@@ -113,7 +113,6 @@ ALL_TOOLCHAINS = {
 	toolchain_ncgen,
 	toolchain_ncpm,
 	toolchain_ncpmz,
-	toolchain_lx386,
 	toolchain_bbct,
 	toolchain_bbctiny,
 	toolchain_bbct6502,
