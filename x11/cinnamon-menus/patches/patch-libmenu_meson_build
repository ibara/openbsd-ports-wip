$OpenBSD$

Index: libmenu/meson.build
--- libmenu/meson.build.orig
+++ libmenu/meson.build
@@ -33,8 +33,8 @@ libmenu_deps = [
 libcinnamon_menus = library(
     'cinnamon-menu-3',
     libmenu_sources,
-    soversion: binary_major_version,
-    version: binary_version,
+    soversion: '0.0',
+    version: '0.0',
     include_directories: include_root,
     dependencies: libmenu_deps,
     install: true,
