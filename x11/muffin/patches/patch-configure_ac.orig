$OpenBSD$

Crashes configure.

Index: configure.ac
--- configure.ac.orig
+++ configure.ac
@@ -463,7 +463,6 @@ if test "x$enable_debug" = "xyes"; then
 fi
 
 # Use gnome-doc-utils:
-GNOME_DOC_INIT([0.8.0])
 
 GTK_DOC_CHECK([1.15], [--flavour no-tmpl])
 
