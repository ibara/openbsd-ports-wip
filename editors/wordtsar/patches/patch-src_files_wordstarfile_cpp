$OpenBSD$

Index: src/files/wordstarfile.cpp
--- src/files/wordstarfile.cpp.orig
+++ src/files/wordstarfile.cpp
@@ -1054,7 +1054,7 @@ void cWordstarFile::HandleChar(wxChar c, size_t &loop)
         if(insert)
         {
             wxString ch = print ;       // this needs to be wxString rather an wxChar because of compiler ambiguity
-            mDocument->Insert(ch) ;
+            mDocument->Insert(std::string(ch.mb_str())) ;
 
             if(mInIndex == true)
             {
@@ -1410,7 +1410,7 @@ void cWordstarFile::BuildFontList(void)
             // get the best (lowest) match
             for(long n = 0; n < nFacenames; n++)
             {
-                size_t value = uiLevenshteinDistance(mFontNames[loop], facenames[n]) ;
+                size_t value = uiLevenshteinDistance(mFontNames[loop], std::string(facenames[n].mb_str())) ;
                 if(value < index)
                 {
                     fontname = facenames[n] ;
