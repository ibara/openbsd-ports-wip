$OpenBSD$

Index: Makefile
--- Makefile.orig
+++ Makefile
@@ -2,7 +2,7 @@
 # Default to 'make -f Makefile.gnu' for Linux and for unknown OS. 
 #
 OS = $(shell uname)
-MAKEFILE = gnu
+MAKEFILE = fip
 
 ifeq ($(OS), Darwin)
     MAKEFILE = osx
