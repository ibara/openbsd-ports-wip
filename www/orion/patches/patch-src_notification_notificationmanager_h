$OpenBSD$

Index: src/notification/notificationmanager.h
--- src/notification/notificationmanager.h.orig
+++ src/notification/notificationmanager.h
@@ -26,7 +26,7 @@
     #include <QQuickWindow>
 #endif
 
-#if defined(Q_OS_MAC) || defined(Q_OS_LINUX)
+#if defined(Q_OS_MAC) || defined(Q_OS_LINUX) || defined(Q_OS_OPENBSD)
     #include "notificationsender.h"
 
 #endif
